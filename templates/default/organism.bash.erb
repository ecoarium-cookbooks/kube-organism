#!/usr/bin/env bash
# Generated by Chef

function minikube() {
  local minikube_path=<%= node[:kube][:minikube][:base_directory] %>/<%= node[:kube][:minikube][:version] %>/minikube
  DEBUG='' $minikube_path $*
}

function kubectl() {
  local kubectl_path=<%= node[:kube][:kubectl][:base_directory] %>/<%= node[:kube][:kubectl][:version] %>/kubectl
  DEBUG='' $kubectl_path $*
}

function helm() {
  local helm_path=<%= node[:kube][:helm][:base_directory] %>/<%= node[:kube][:helm][:version] %>/helm
  DEBUG='' $helm_path $*
}

function tiller() {
  local tiller_path=<%= node[:kube][:tiller][:base_directory] %>/<%= node[:kube][:tiller][:version] %>/tiller
  DEBUG='' $tiller_path $*
}
