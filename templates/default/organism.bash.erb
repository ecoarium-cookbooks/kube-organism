#!/usr/bin/env bash
# Generated by Chef

function minikube() {
  local minikube_path=<%= node[:kube][:minikube][:base_directory] %>/<%= node[:kube][:minikube][:version] %>/minikube
  DEBUG='' $minikube_path $*
}

function kubectl() {
  local kubectl_path=<%= node[:kube][:kubectl][:base_directory] %>/<%= node[:kube][:kubectl][:version] %>/kubectl
  DEBUG='' $kubectl_path $*
}
